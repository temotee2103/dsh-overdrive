{
  "name": "dsh-overdrive",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @dsh-overdrive/sdk && npm run build --workspaces --if-present",
    "test": "vitest run",
    "e2e": "node test/e2e.mjs"
  },
  "devDependencies": {
    "@deepseek-ai/dsh": "^0.1.0-rc.6",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "license": "MIT",
  "description": "dsh-overdrive: 让 DeepSeek Harness 变成多平台聊天智能体",
  "repository": {
    "type": "git",
    "url": "https://github.com/<owner>/dsh-overdrive.git"
  },
  "publishConfig": {
    "access": "public"
  }
}
